import { Copy, Users } from "lucide-react";
import type { ClassRecord } from "@/lib/types/database";

export function ClassList({ classes }: { classes: ClassRecord[] }) {
  if (!classes.length) return <div className="card p-8 text-center"><Users className="mx-auto text-[#125cdb]" size={36}/><h2 className="mt-3 text-xl font-black">Create your first class</h2><p className="mt-2 text-[#62708a]">A unique enrollment code will be generated automatically.</p></div>;
  return <div className="grid gap-5 md:grid-cols-2">{classes.map((classroom) => <article key={classroom.id} className="card p-6"><div className="flex items-start justify-between"><div><p className="text-sm font-bold text-[#125cdb]">Grade {classroom.grade} · Section {classroom.section}</p><h2 className="mt-1 text-xl font-black">{classroom.name}</h2></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-sm font-bold text-[#16885a]">{classroom.student_count ?? 0} students</span></div><div className="mt-5 flex items-center justify-between rounded-2xl bg-[#eff5ff] p-4"><div><p className="text-xs font-bold uppercase tracking-wider text-[#62708a]">Class code</p><p className="mt-1 font-mono text-lg font-black text-[#125cdb]">{classroom.class_code}</p></div><Copy size={19} className="text-[#125cdb]"/></div><div className="mt-5 flex gap-3"><a href={`/teacher/classes/${classroom.id}`} className="rounded-xl bg-[#125cdb] px-4 py-2 text-sm font-bold text-white">View students</a><a href={`/teacher/classes/${classroom.id}/manage`} className="rounded-xl border border-[#dce5f2] px-4 py-2 text-sm font-bold text-[#31445f]">Manage class</a></div></article>)}</div>;
}
